export interface Destination {
    id: number,
    nameDestination: string,
    address: string,
    numberResidents: number,
    numberHotels: number,
    averageSalary: number,
    surface: number,
    isActived: boolean;
    imgUrl: string;
}